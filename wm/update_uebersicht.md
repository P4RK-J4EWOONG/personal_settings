# Refreshing Uebersicht

If you need to manually refresh Uebersicht, you can do so by running the following command in your terminal:

```bash
osascript -e 'tell application id "tracesOf.Uebersicht" to refresh'
```
